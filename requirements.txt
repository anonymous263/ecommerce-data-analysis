# Direct dependencies only. For a byte-exact reproduction of the environment
# this project was built and tested against, use requirements-lock.txt instead:
#
#     pip install -r requirements-lock.txt
#
# Regenerate the lock with:  pip freeze > requirements-lock.txt

# --- Extract / Load (Python does EL only — no business logic) ---
httpx==0.28.1               # WooCommerce REST client
python-dotenv==1.2.2        # .env loading (secrets never live in source)
PyYAML==6.0.3               # config/sites.yaml
pandas==3.0.3               # manual cost-sheet parsing
SQLAlchemy==2.0.50          # engine + raw-schema upserts
psycopg2-binary==2.9.12     # Postgres driver

# --- Transforms (dbt owns every transform) ---
dbt-core==1.10.21
dbt-postgres==1.10.0

# --- Tests ---
pytest==9.0.3
